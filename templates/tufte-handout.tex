$passoptions.latex()$
\documentclass[
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}

\usepackage{xcolor}

\usepackage{amsmath,amssymb}


$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
$endif$

$fonts.latex()$
$common.latex()$

$for(header-includes)$
$header-includes$
$endfor$

$after-header-includes.latex()$
$hypersetup.latex()$

\usepackage{etoolbox}

%%%%%%%%%%%%%%%% IGOR %%%%%%%%%%%%%%%%%%


\usepackage{morefloats}

\usepackage{float}
\usepackage{tcolorbox}

\usepackage{changepage}
\usepackage[a4paper, lmargin=0.5in, rmargin=0.5in, bmargin=0.3in, tmargin=0.65in, includefoot]{geometry}

\usepackage{tcolorbox}

\definecolor{green}{HTML}{a1c3a1}
\newtcolorbox{boxGreen}[1]{%
  colframe=green, 
  coltitle=black,
  colback=white,
  arc=0mm,
  title=#1,
  width=0.8\textwidth
}

\definecolor{blueish}{HTML}{b5c1ea}
\newtcolorbox{boxBlue}[1]{%
  colframe=blueish, 
  coltitle=black,
  colback=white,
  arc=0mm,
  title=#1,
  width=0.8\textwidth
}


\definecolor{yellow}{HTML}{FFF59D}
\newtcolorbox{boxYellow}[1]{%
  colframe=yellow, 
  coltitle=black,
  colback=white,
  arc=0mm,
  title=#1,
  width=0.8\textwidth
}

\usepackage{algorithm2e}
\RestyleAlgo{ruled}

\title{$title$}
\author{$for(author)$$author$$sep$ \and $endfor$}


\makeatletter
\let\newtitle\@title
\let\newauthor\@author
\let\newdate\@date
\makeatother

\let\origfigure\figure
\let\endorigfigure\endfigure
\renewenvironment{figure}[1][2] {
    \origfigure[H]
} {
    \endorigfigure
}


\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancypagestyle{first}{
\fancyhead[L]{}
\fancyhead[R]{}
}


\fancyfoot[R]{\thepage}
\fancyfoot[L]{
\raisebox{-0.33\height}{\includegraphics[height=2em]{templates/by-nc-sa.png}} -- © \newauthor
}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.4pt}
\renewcommand{\arraystretch}{1.5}


\fancyhead[L]{\newtitle} 
\fancyhead[R]{$versao$}



% \usepackage[skip=10pt plus1pt]{parskip}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\begin{document}
$if(title)$
{\Huge\newtitle}

{\large $subtitle$ - $versao$}

Última atualização: \newdate
% \maketitle

\par\noindent\rule{\textwidth}{0.4pt}

$endif$

\thispagestyle{first}

$for(include-before)$
$include-before$

$endfor$

$body$

$for(include-after)$
$include-after$

$endfor$



$if(natbib)$
$if(bibliography)$
$if(biblio-title)$
$if(has-chapters)$
\renewcommand\bibname{$biblio-title$}
$else$
\renewcommand\refname{$biblio-title$}
$endif$
$endif$
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

$endif$
$endif$
$if(biblatex)$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$

$endif$

\end{document}


% {
% $if(colorlinks)$
% \hypersetup{linkcolor=$if(toccolor)$$toccolor$$else$$endif$}
% $endif$
% \setcounter{tocdepth}{$toc-depth$}
% \tableofcontents
% }


% $if(has-frontmatter)$
% \mainmatter
% $endif$


% $if(has-frontmatter)$
% \backmatter
% $endif$
% $if(natbib)$
% $if(bibliography)$
% $if(biblio-title)$
% $if(has-chapters)$
% \renewcommand\bibname{$biblio-title$}
% $else$
% \renewcommand\refname{$biblio-title$}
% $endif$
% $endif$
% \bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

% $endif$
% $endif$
% $if(biblatex)$
% \printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$

% $endif$



